<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=" com.edge.stocks.material.ck.dao.Mat_CK_StockDao">
	<!-- 定义缓存 一般是一级缓存，如果用同一个sqlsession 那么相同查询直接会从缓存中查找 <cache size="1024" flushInterval="60000" 
		eviction="LRU" readOnly="false"></cache> -->

	<!--分页显示出库的库存 -->
	<select id="mat_CK_StockList" parameterType="ERP_MatStock_QueryVo" resultType="ERP_Material_Stock">
		select * from (select row_.*, rownum rownum_ from (select * from
		erp_material_Stock order by material_Id desc ) row_
		<where>
			rownum &lt;= #{rows} and cknumber!=0
		</where>
		)
		<where>
			rownum_ &gt;= #{page}
		</where>
	</select>

	<!--查询出库库存总数量 -->
	<select id="matCKStockCount"  parameterType="ERP_MatStock_QueryVo" resultType="Integer" >
		select count(1) from erp_material_Stock 
	</select>
	
	<!--加载所有未出库的库存  -->
	<select id="allWckMaterialStock" resultType="ERP_Material_Stock">
		select s.* from erp_material_Stock s
		left join erp_raw_Material p on s.material=p.raw_Material_Id
		<where>
		 p.is_ck=0 and (nvl(s.rknumber,0)-nvl(s.cknumber,0))!=0
		</where>
	</select>
	
	<!--加载某一库存下已出库的成品并获得成品数量  -->
	<select id="yckCount" parameterType="Integer" resultType="Integer">
		select count(1) from erp_material_Stocks_Record
		<where>
			stock=#{stock} and record_Type=1
		</where>
	</select>
	
	<!--加载当前成品的总出库数量  -->
	<select id="totalYckCount" parameterType="Integer" resultType="Integer">
		select count(1) from erp_material_Stocks_Record
		<where>
			material=#{material} and record_Type=1
		</where>
	</select>
	
</mapper>